import { fetchNui } from '../../utils/fetchNui';

const InventoryInfo: React.FC = () => {
  // Title moved to LeftInventory for alignment — this component now empty
  return null;
};

export default InventoryInfo;
